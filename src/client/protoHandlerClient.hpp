#include <string>

class ProtoHandler{

   public:
     void protoMethod(std::string &message, std::string &messageHeader, int &lValue, int &rValue );
	 void protoPrint(std::string &message);
};
